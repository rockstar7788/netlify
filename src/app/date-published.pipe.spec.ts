import { DatePublishedPipe } from './date-published.pipe';

describe('DatePublishedPipe', () => {
  it('create an instance', () => {
    const pipe = new DatePublishedPipe();
    expect(pipe).toBeTruthy();
  });
});
